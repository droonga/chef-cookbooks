<% engine = node.droonga.engine %>
<source>
  type forward
  port <%= engine.port %>
</source>
<match <%= engine.tag %>.message>
  name <%= engine.host %>localhost:<%= engine.port %>/<%= engine.tag %>
  type droonga
</match>
<match output.message>
  type stdout
</match>
